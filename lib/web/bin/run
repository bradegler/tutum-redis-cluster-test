#!/usr/bin/env node

/**
  * Module dependencies.
  */
var debug = require('debug')('app:web');
var port = 5000;

// app

var app = require('../lib');

// bind

app.listen(port);

debug('started app on port %d', port);
